/**
 * The Home Screen steps for installing a Journal.IO widget.
 *
 * Shared by the Settings "How to add a widget" sheet and the onboarding widget
 * step so the two never drift. Only the copy is shared — each surface keeps its
 * own layout.
 */
const ADD_WIDGET_STEPS = [
  'Touch and hold an empty area on your Home Screen.',
  'Tap +, then tap Add Widget.',
  'Search for Journal.IO.',
  'Choose a size and tap Add Widget.',
];

export { ADD_WIDGET_STEPS };
